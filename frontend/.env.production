# Main API base URL used by the built front end.
# Empty = same origin (nginx serves the app and proxies /api to
# the backend container).
# Point it at a full URL to call a remote API directly, e.g.
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
