# Main API base URL used by the front end.
# Empty = same origin (requests go to the Vite dev server, which
# proxies /api to the backend container).
# Point it at a full URL to call a remote API directly, e.g.
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
